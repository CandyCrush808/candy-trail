export function playSound(enabled:boolean,type:"click"|"reward"|"complete"="click"){if(!enabled||typeof window==="undefined")return;try{const AudioContextClass=window.AudioContext;const ctx=new AudioContextClass();const oscillator=ctx.createOscillator();const gain=ctx.createGain();oscillator.type="sine";oscillator.frequency.value=type==="complete"?720:type==="reward"?580:420;gain.gain.setValueAtTime(.04,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.001,ctx.currentTime+.14);oscillator.connect(gain);gain.connect(ctx.destination);oscillator.start();oscillator.stop(ctx.currentTime+.14)}catch{}}
